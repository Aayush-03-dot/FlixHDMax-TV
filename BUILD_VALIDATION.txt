FlixHDMax TV v4 validation

Passed:
- TypeScript project compilation (tsc -b)
- ESLint validation for TV source, App routing, and Vite configuration
- Python compilation for the hosted-video playback route
- Android manifest XML parsing
- GitHub Actions workflow YAML parsing
- Package and TV manifest JSON parsing
- CSS brace validation
- Native keyboard bridge static validation
- TV focus-lane static validation
- AndroidX / Media3 / compile SDK compatibility configuration check
- Clean-package scan for node_modules, build output, Gradle cache, APK files, and private .env files

Not executed in this container:
- Final Vite bundle, because the available dependency cache contained a Windows-specific Rolldown native package. Run npm ci and npm run build on Windows.
- Android APK compilation, because the Android SDK is not installed here. Use the included GitHub Actions workflow.
